# Baseline of tests allowed to fail. One entry per line, formatted as:
#
#   tests/<path>.test.js :: <full test name>
#
# or, for a suite that cannot even be collected (missing module, broken import):
#
#   tests/<path>.test.js :: <suite failed to collect>
#
# Empty lines and lines starting with '#' are ignored.
#
# EMPTY AS OF v0.5.82: `cd tests && npx vitest run` is fully green
# (2659 tests, 2545 pass, 114 skipped, 0 fail, 0 broken suites). Anything that
# fails from now on is a regression — do not re-add entries to silence the gate
# without fixing the cause first.
